# ============================================================
# ton-llm-key — Environment Variables
# §10 HANDOFF.md
# ============================================================
# Copy to .env.local for local dev. Never commit .env.local.

# ---- LiteLLM (§1 D5) ----
LITELLM_BASE_URL=https://api2.ai.tokamak.network
LITELLM_MASTER_KEY=sk-...

# ---- Ethereum RPC (§2) ----
# Alchemy or Infura mainnet endpoint (free tier sufficient)
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>
# Optional: Sepolia testnet for development
RPC_URL_SEPOLIA=https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>

# ---- Upstash Redis (§1 D9) ----
# Vercel KV는 2024-12 종료 → Vercel Marketplace에서 Upstash Redis 연결 후 자동 주입.
# 연결 방법: Vercel Dashboard → Storage → Connect Store → Upstash Redis (Marketplace)
# 로컬 개발: vercel env pull .env.local 실행 후 자동 반영
KV_REST_API_URL=...
KV_REST_API_TOKEN=...
KV_REST_API_READ_ONLY_TOKEN=...

# ---- Session (§1 D4) ----
# Generate with: openssl rand -base64 32
SESSION_SECRET=<openssl rand -base64 32>

# ---- Staking threshold (§1 D2) ----
MIN_TON=10
